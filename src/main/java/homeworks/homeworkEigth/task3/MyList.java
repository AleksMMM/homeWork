package homeworks.homeworkEigth.task3;

public interface MyList {

    void add(int val); //добавляет элемент в список, в качестве основы для хранения чисел взять массив/ArrayList
    int get(int index);
    int size();
    boolean contains(int val);
}
